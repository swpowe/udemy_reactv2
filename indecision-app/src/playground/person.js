// adult returns true if 18+ otherwise false

// canDrink true if 21 and over otherwise false

console.log('person.js is running');

export const adult = (age) => age >= 18;

export const canDrink = (age) => age >= 21;

const senior = (age) => age >= 65;

export default senior;